This program evaluates expressions in a very simple integer expression language.

To build:
	
	./gradlew build

To execute (pre-compiled jar at root):

	java -jar calculator.jar "add(2, 2)" "123"    
